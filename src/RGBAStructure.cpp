#include "RGBAStructure.h"

RGBAStructure::RGBAStructure(const uint8_t& red, const uint8_t& green, const uint8_t& blue, const uint8_t& alpha)
    : red(red), green(green), blue(blue), alpha(alpha)
{
}